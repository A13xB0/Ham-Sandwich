(function (H) {
  'use strict';

  /** Legacy hook — banner removed in teleprompter-first UI. */
  function renderBanner() {}

  H.progressive = { renderBanner };
})(window.HamApp = window.HamApp || {});
